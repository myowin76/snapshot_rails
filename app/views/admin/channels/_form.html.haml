= form_for @admin_channel do |f|
  - if @admin_channel.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_channel.errors.count, "error")} prohibited this admin_channel from being saved:"
      %ul
        - @admin_channel.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
