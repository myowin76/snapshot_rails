= form_for @admin_theme do |f|
  - if @admin_theme.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_theme.errors.count, "error")} prohibited this admin_theme from being saved:"
      %ul
        - @admin_theme.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
